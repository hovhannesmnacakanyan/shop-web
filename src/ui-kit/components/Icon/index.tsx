import React from "react";
import {
  AccountCircle as MUIAccountCircle,
  Add as MUIAdd,
  ArrowBack as MUIArrowBack,
  Category as MUICategory,
  CheckCircle as MUICheckCircle,
  ChevronRight as MUIChevronRight,
  Close as MUIClose,
  Collections as MUICollections,
  Delete as MUIDelete,
  Done as MUIDone,
  Edit as MUIEdit,
  Error as MUIError,
  FilterList as MUIFilterList,
  Folder as MUIFolder,
  InsertPhoto as MUIInsertPhoto,
  KeyboardArrowDown as MUIKeyboardArrowDown,
  KeyboardArrowRight as MUIKeyboardArrowRight,
  KeyboardArrowUp as MUIKeyboardArrowUp,
  KeyboardBackspace as MUIKeyboardBackspace,
  Layers as MUILayers,
  Lock as MUILock,
  Logout as MUILogout,
  MoreHoriz as MUIMoreHoriz,
  MoreVert as MUIMoreVert,
  People as MUIPeople,
  Person as MUIPerson,
  PersonAdd as MUIPersonAdd,
  Search as MUISearch,
  Settings as MUISettings,
  ShoppingCart as MUIShoppingCart,
  Sort as MUISort,
} from "@mui/icons-material";
import { SvgIconProps } from "@mui/material";

export const KeyboardBackspace = (props: SvgIconProps) => {
  return <MUIKeyboardBackspace {...props} />;
};

export const KeyboardArrowRight = (props: SvgIconProps) => {
  return <MUIKeyboardArrowRight {...props} />;
};

export const ChevronRight = (props: SvgIconProps) => {
  return <MUIChevronRight {...props} />;
};

export const Edit = (props: SvgIconProps) => {
  return <MUIEdit {...props} />;
};

export const Category = (props: SvgIconProps) => {
  return <MUICategory {...props} />;
};

export const Add = (props: SvgIconProps) => {
  return <MUIAdd {...props} />;
};

export const Close = (props: SvgIconProps) => {
  return <MUIClose {...props} />;
};

export const KeyboardArrowDown = (props: SvgIconProps) => {
  return <MUIKeyboardArrowDown {...props} />;
};

export const KeyboardArrowUp = (props: SvgIconProps) => {
  return <MUIKeyboardArrowUp {...props} />;
};

export const Search = (props: SvgIconProps) => {
  return <MUISearch {...props} />;
};

export const Error = (props: SvgIconProps) => {
  return <MUIError {...props} />;
};

export const People = (props: SvgIconProps) => {
  return <MUIPeople {...props} />;
};

export const AccountCircle = (props: SvgIconProps) => {
  return <MUIAccountCircle {...props} />;
};

export const Logout = (props: SvgIconProps) => {
  return <MUILogout {...props} />;
};

export const Settings = (props: SvgIconProps) => {
  return <MUISettings {...props} />;
};

export const Collections = (props: SvgIconProps) => {
  return <MUICollections {...props} />;
};

export const Folder = (props: SvgIconProps) => {
  return <MUIFolder {...props} />;
};
export const SortIcon = (props: SvgIconProps) => {
  return <MUISort {...props} />;
};
export const InsertPhoto = (props: SvgIconProps) => {
  return <MUIInsertPhoto {...props} />;
};

export const Layers = (props: SvgIconProps) => {
  return <MUILayers {...props} />;
};

export const ShoppingCart = (props: SvgIconProps) => {
  return <MUIShoppingCart {...props} />;
};

export const ArrowBack = (props: SvgIconProps) => {
  return <MUIArrowBack {...props} />;
};

export const Done = (props: SvgIconProps) => {
  return <MUIDone {...props} />;
};

export const PersonAdd = (props: SvgIconProps) => {
  return <MUIPersonAdd {...props} />;
};

export const Person = (props: SvgIconProps) => {
  return <MUIPerson {...props} />;
};

export const FilterList = (props: SvgIconProps) => {
  return <MUIFilterList {...props} />;
};

export const MoreHoriz = (props: SvgIconProps) => {
  return <MUIMoreHoriz {...props} />;
};
export const MoreVert = (props: SvgIconProps) => {
  return <MUIMoreVert {...props} />;
};
export const Lock = (props: SvgIconProps) => {
  return <MUILock {...props} />;
};

export const CheckCircle = (props: SvgIconProps) => {
  return <MUICheckCircle {...props} />;
};

export const Delete = (props: SvgIconProps) => {
  return <MUIDelete {...props} />;
};
